# Core dependencies
requests
python-dotenv

# Optional: spreadsheet support (source_reader.read_excel)
openpyxl

# Optional: image manipulation (image_utils, examples/extract_images)
# Pillow

# Optional: PDF image/content extraction (examples/extract_images, tools/extract_content)
# PyMuPDF

# Optional: DOCX content extraction (tools/extract_content)
# python-docx
